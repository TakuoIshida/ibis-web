// 立ち位置：Storeに保存されているstateを参照する関数を提供します。
// 使い方：selectorsファイル内で定義した関数をコンポーネントでimportして使います。
// import { createSelector } from "reselect";
// import State from 're-ducks/store/types'

// const usersSelector = (state: State) => state.users;

// export const getUserId = createSelector(
//     [usersSelector],
//     state => state.uid
// );

// import { createSelector } from "reselect";
// import State from './types'

// const counterSelector = (state: State) => state.count;

// export const getUserId = createSelector(
//     [usersSelector],
//     state => state.uid
// );