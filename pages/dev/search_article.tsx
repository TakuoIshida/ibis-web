import { BASE_URL, API_ROUTE } from "../../constants/settings"
import { getFetch, postFetch } from '../../util/common'
import { get_articles_search } from '../../util/sample-data'

const SearchArticle = ({ searchList }: any) => {
  console.log("saerch page ", searchList)
  return (
    <div>
      <p></p>
    </div>
  )
}

export default SearchArticle
